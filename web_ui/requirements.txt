# Web UI dependencies — layered on top of the core runtime requirements.
# pandas / pyyaml / openpyxl / requests all come from ../requirements.txt so they
# are not re-pinned here (re-pinning caused resolver conflicts in CI/Docker).
-r ../requirements.txt

Flask>=3.0,<4
